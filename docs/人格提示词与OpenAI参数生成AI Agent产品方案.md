# 人格提示词与OpenAI参数生成AI Agent产品方案

核心定位：一款「输入人格需求，输出可直接复制使用」的AI工具——用户只需描述人格类型（如专业药品助手、治愈陪伴、毒舌顾问），Agent自动生成结构化人格提示词（含大五参数/MBTI）、配套OpenAI API参数，且支持自定义调整、场景适配、一键复制，适配Dify、直接API调用等多场景，主打“零门槛、高适配、可落地”。

# 一、产品核心目标（解决用户痛点）

- 痛点1：手动写人格提示词不规范、易OOC，不懂大五参数/OpenAI参数搭配 → 解决方案：自动生成标准化、防漂移的提示词+参数组合

- 痛点2：不同场景（医疗、陪伴、客服）参数适配难，反复调试 → 解决方案：按场景预设最优参数，支持自定义微调

- 痛点3：提示词与参数脱节，人格不稳定 → 解决方案：绑定人格特质与参数逻辑，确保“提示词定人格，参数保稳定”

- 核心目标：让非技术/技术用户，都能1分钟生成可直接使用的“人格提示词+OpenAI参数”，无需专业知识。

# 二、产品核心功能（分3大模块，落地性极强）

## 模块1：人格需求输入（降低用户操作成本）

核心：让用户用“自然语言描述”，无需懂大五/MBTI，Agent自动解析人格核心特质。

### 输入方式（3种，覆盖所有用户）

1. 快速选型：预设10+高频人格模板（专业严谨型、治愈陪伴型、活泼助手型、毒舌顾问型、药品/医疗专用型、客服型、游戏NPC型等），用户直接选择，无需输入。

2. 自然语言描述：用户输入如“我要一个专业、严谨、不胡编乱造，只按说明书回答的药品查询助手”，Agent自动提取关键词（专业、严谨、不编造、照本宣科）。

3. 精准配置：高级用户可直接输入大五参数（O/C/E/A/N）、MBTI类型，Agent基于此优化提示词和参数。

### 辅助输入（可选，提升适配性）

- 使用场景：下拉选择（Dify配置、直接API调用、ChatGPT插件、其他平台）

- 语气要求：下拉选择（正式/口语/简洁/温柔/犀利）

- 防OOC要求：勾选（强约束/普通约束/无约束）

- 禁止内容：用户可输入禁止话术（如“不做医疗诊断、不胡编药品疗效”）

## 模块2：核心输出（可直接复制使用，核心价值）

输出内容按“场景适配”分类，确保用户复制后无需修改，直接可用。

### 基础输出（所有场景通用）

1. 结构化人格提示词（分3部分，防OOC+高适配）

    - 人格基线：身份、核心使命、行为边界（如“你是专业药品查询助手，只依据说明书回答，不做医疗诊断”）

    - 人格特质：大五参数（明确数值）+ 性格描述（对应参数，让提示词更具体）

    - 强约束规则：防OOC、禁止行为、回答规范（如“每轮回答前检查是否符合人格，不符合则重写；不联想、不推断”）

2. 配套OpenAI API参数（JSON格式，可直接复制到代码/工具）

    - 核心参数：temperature、top_p、frequency_penalty、presence_penalty（按人格特质精准匹配）

    - 辅助参数：max_tokens（按场景预设，如药品查询设1024，陪伴设512）、logit_bias（默认空，可自定义）

### 场景化输出（差异化适配）

- Dify适配版：提示词按Dify“系统提示词”格式优化，参数对应Dify模型参数面板，可直接粘贴到Dify后台。

- API调用版：提示词整合到OpenAI API的messages[0].system，参数整合到请求体JSON，提供完整调用示例（Python/Java）。

- 高级版：支持生成Few-shot示例对话（2-3组，强化人格风格）、记忆设置建议（如Dify记忆窗口、每N轮人格锚点）。

### 输出交互

- 一键复制：每个输出模块（提示词、参数、代码示例）单独设置复制按钮，点击直接复制。

- 实时预览：生成后可预览“人格模拟回复”，验证人格是否符合预期（如输入用户问题，预览Agent回复风格）。

## 模块3：自定义调整（满足个性化需求）

核心：允许用户微调，兼顾标准化与个性化，避免“一刀切”。

1. 提示词微调：直接编辑生成的提示词，实时同步预览效果。

2. 参数微调：滑动条调整temperature、top_p等参数，实时显示参数说明（如“temperature=0.1：极度稳定，不发散”），调整后自动同步提示词的约束强度。

3. 人格微调：调整大五参数数值，提示词和参数会自动联动更新（如提高E值，提示词会增加“热情、主动”，temperature可适当提高）。

4. 保存与导出：用户可保存生成的“人格方案”（命名、分类），支持导出为txt/JSON格式，方便后续复用。

# 三、核心Agent架构（产品底层逻辑，确保生成精准）

Agent分为3个核心节点，联动工作，确保“人格提示词”与“OpenAI参数”高度匹配，不脱节。

## 节点1：需求解析Agent（输入→特质提取）

核心功能：解析用户输入（自然语言/模板选择/参数输入），提取核心人格特质，映射为标准化大五参数和行为规则。

逻辑示例：用户输入“药品查询助手，专业、严谨、不胡编” → 提取特质（严谨、务实、不发散、情绪稳定）→ 映射大五参数（O=20、C=95、E=10、A=70、N=0）→ 确定行为规则（只按说明书、不做诊断、不联想）。

关键：内置“人格特质-大五参数”映射库，覆盖所有高频场景，确保提取精准。

## 节点2：提示词生成Agent（特质→标准化提示词）

核心功能：基于解析出的大五参数、行为规则、场景需求，生成结构化、防OOC的提示词。

生成规则（确保专业、可落地）：
      1.  开头强约束：明确身份和核心规则，优先级最高；
      2.  中间人格描述：绑定大五参数，让提示词有量化依据（如“大五人格：O=20%，务实不发散；C=95%，极度严谨”）；
      3.  结尾防OOC：加入校验规则，确保Agent不跑偏；
      4.  场景适配：根据用户选择的场景（如Dify），调整提示词格式和语气。

## 节点3：参数匹配Agent（人格→OpenAI参数）

核心功能：基于大五参数和人格特质，匹配最优OpenAI参数，确保“人格稳定”与“参数合理”联动。

核心匹配逻辑（核心重点，对应你之前的药品Agent经验）：

|人格特质|大五参数特征|OpenAI参数建议|适用场景|
|---|---|---|---|
|专业严谨、不发散|C高（85-100）、O低（0-30）、N=0|temperature=0.1-0.2，top_p=0.1-0.2，penalty=0|药品、医疗、专业咨询|
|治愈陪伴、自然|A高（85-100）、E中（30-50）、N低（0-10）|temperature=0.4-0.5，top_p=0.5-0.6，penalty=0.1|陪伴、情感倾听|
|活泼创意、话多|O高（70-100）、E高（70-100）、C中（40-60）|temperature=0.7-0.9，top_p=0.8，penalty=0.2|聊天搭子、主播、NPC|
|理性犀利、不讨好|A低（20-40）、C高（70-90）、N=0|temperature=0.2-0.3，top_p=0.3，penalty=0|顾问、批判性分析|
关键：参数匹配Agent会根据用户微调的大五参数，自动调整对应OpenAI参数，确保联动一致（如用户提高C值，temperature自动降低）。

# 四、产品差异化优势（比普通工具更实用）

1. 人格与参数强绑定：不是“提示词和参数分开生成”，而是基于同一人格特质联动生成，避免脱节（如专业严谨人格，提示词强约束+参数极低，双重保障不OOC）。

2. 场景化适配：针对Dify、API调用等高频场景优化输出，用户复制即能用，无需二次修改（解决普通工具“生成后还要手动调整格式”的痛点）。

3. 医疗/专业场景适配：内置药品、医疗等敏感场景的专属模板和约束规则（如“不做医疗诊断、不胡编疗效”），符合合规要求，尤其适配你熟悉的医疗类Agent。

4. 零门槛：无需懂大五模型、OpenAI参数，小白也能生成专业方案；高级用户可精准微调，兼顾易用性和专业性。

# 五、落地优先级（先做核心功能，快速上线）

## 第一阶段（1-2周，最小可行产品MVP）

- 核心功能：预设10+高频人格模板、自然语言输入解析、生成基础提示词+OpenAI参数、一键复制。

- 重点适配：Dify、OpenAI API调用场景。

- 核心逻辑：完成3个Agent节点的基础联动，确保生成精准（重点覆盖专业严谨型，贴合你的药品Agent经验）。

## 第二阶段（2-3周，优化体验）

- 新增功能：自定义微调（提示词、参数、大五参数）、实时预览、保存/导出方案。

- 优化：扩充人格模板（增加游戏NPC、客服等）、优化需求解析精度、增加Few-shot示例生成。

## 第三阶段（3-4周，拓展功能）

- 新增：团队协作（共享人格方案）、API接口开放（支持第三方工具对接）、多语言支持。

- 优化：基于用户使用数据，优化参数匹配逻辑，提升生成精准度。

# 六、关键细节（确保产品好用、合规）

1. 合规性：针对医疗、法律等敏感场景，提示词中强制加入“不提供专业诊断/建议”等约束，避免风险（贴合你药品Agent的合规需求）。

2. 参数说明：每个参数都添加简单易懂的说明，让用户知道“为什么这么设”，降低认知成本。

3. 容错性：用户输入模糊时（如“想要一个靠谱的助手”），Agent自动推荐最接近的人格模板，并提示用户补充细节。

4. 性能：生成速度控制在1秒内，预览功能实时响应，提升用户体验。

# 七、总结

这款产品的核心竞争力的是“精准联动+场景适配+零门槛”，依托你熟悉的人格控制（大五参数）、OpenAI参数匹配、医疗场景合规经验，可快速打造差异化优势。优先落地MVP版本，聚焦“专业严谨型”“医疗/药品专用型”模板（你的核心优势场景），快速验证市场需求，再逐步拓展其他人格类型和功能。
> （注：文档部分内容可能由 AI 生成）
